services:
  - type: web
    name: auditoria-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: nome-do-banco-no-render
          property: connectionString
